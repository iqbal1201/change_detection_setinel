# Core geospatial
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyproj>=3.6

# Scientific computing (numpy, scipy already installed)
scikit-learn>=1.3
scikit-image>=0.21
opencv-python>=4.8

# Visualization
folium>=0.15
matplotlib>=3.7  # already installed

# Deep learning (install separately — see instructions below)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
transformers>=4.40
timm>=1.0

# SAM2 (install separately)
# pip install git+https://github.com/facebookresearch/sam2.git

# Utilities (tqdm already installed)
tqdm>=4.65

# ── PyTorch install for CUDA 13.2 (use cu124 wheels — backward compatible) ────
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# pip install git+https://github.com/facebookresearch/sam2.git
# pip install rasterio geopandas shapely fiona pyproj scikit-learn scikit-image opencv-python folium transformers timm
